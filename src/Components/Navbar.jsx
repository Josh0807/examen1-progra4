function Navbar() {
  return (
    <header className="navbar">
      <div className="brand">AutoParts CR</div>
      <nav className="nav-links">
        <a href="#inicio">Inicio</a>
        <a href="#catalogo">Catalogo</a>
        <a href="#contacto">Contacto</a>
      </nav>
    </header>
  );
}

export default Navbar;
